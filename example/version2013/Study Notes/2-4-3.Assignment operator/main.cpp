
#include <iostream>

using namespace std;

int main(int argc, char* argv[]){

	cout << "*****Assignment Operator*****" << endl;
	cout << endl;
	/*
	運算子		例 子	說 明
	+=			a += b 	a = a + b
	-=			a -= b	a = a - b
	*=			a *= b	a = a * b
	/=			a /= b	a = a / b
	%=			a %= b	a = a % b
	&=			a &= b	a = a & b
	|=			a |= b	a = a | b
	^=			a ^= b	a = a ^ b
	<<=			a <<= b	a = a << b
	>>=			a >>= b	a = a >> b
*/

	cout << endl;
	system("pause");
	return 0;
}